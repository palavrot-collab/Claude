import './globals.css';

export const metadata = {
  title: 'Manager LMS',
};

// Root layout is minimal; the locale layout sets <html lang/dir>.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
